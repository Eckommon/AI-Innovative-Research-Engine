---
id: DEC-122
type: decision
created: 2026-09-09
status: ACTIVE
---

# DEC-122 — Finalize US-AIR-F01 as JOIN_READY PASS
# DEC-122 — US-AIR-F01을 JOIN_READY PASS로 종료

## Decision / 결정

Finalize Issue #88 / `US-AIR-F01` as:

**`PASS_US_AIR_AIRPORT_WEATHER_JOIN_READY`**

and close the feasibility issue after the durable checkpoint commit is verified. / durable checkpoint commit 검증 후 feasibility Issue #88을 종결한다.

## Basis / 근거

Successful outcome-blind Run `34218434530` and its committed manifests establish:
- 349 origin AirportIDs present in all twelve 2025 BTS months;
- 528 annual AirportID/SeqID pairs;
- 337 all-month airports passing the frozen full-year spatial identity rule;
- 340 unique NOAA stations in that spatial-qualified set;
- 263 final airports and 263 unique NOAA stations with complete 365-date source support;
- 95,995 final station×calendar-date source keys.

All frozen minimums (>=50 airports, >=40 stations, complete 2025 date support) are met without changing the 10 km cap, repairing ties manually, substituting sources, or opening weather/delay effects. / 10km·tie 제외·source 고정·결과 비노출 규칙을 바꾸지 않고 최소 기준을 충족했다.

## Temporal adjudication / 시간 판정

The deterministic first-use join is the same YYYY-MM-DD calendar label between BTS `FlightDate` and NOAA DATE. NOAA hourly time is LST without DST adjustment, so this daily-label contract does not imply equal physical 24-hour windows during DST. Any hourly/sub-daily experiment requires a separately frozen conversion contract. / daily label 결합만 허용하며 DST의 물리적 24시간 동일성은 주장하지 않는다.

## Outcome eligibility / outcome 자격

Preserve the pre-effect contract:
- primary future continuous outcome = `DepDelayMinutes`;
- `WeatherDelay` is not a primary outcome;
- Duplicate and Cancelled rows are excluded from a future continuous-delay denominator;
- non-cancelled Diverted rows may remain only with nonblank `DepDelayMinutes`.

No delay magnitude was used to make this decision. / 본 결정에 지연값 크기는 사용하지 않았다.

## Promotion boundary / 승격 경계

**JOIN_READY ≠ EXPERIMENT_READY.**

Do not automatically create or execute E01. Return to Stage 0 portfolio control. / E01 자동 생성·실행을 금지하고 Stage 0로 복귀한다.

A later E01 is authorized only by a new preregistration that fixes the scientific question, weather variable/quality semantics, aggregation denominator, baseline, evaluation split, dependence-aware inference, falsification and minimum useful improvement before effect exposure. Novelty and practical utility remain UNKNOWN. / 향후 E01은 효과 노출 전 별도 사전등록이 필요하며 신규성·실용성은 UNKNOWN이다.

## Cost / 비용

Incremental monetary cost remains **0 USD**. COST-001 and RAW-001 remain mandatory.
