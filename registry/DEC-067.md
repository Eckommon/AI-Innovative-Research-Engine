---
id: DEC-067
type: decision
state: ACTIVE
created: 2026-08-23
source_of_truth: github
research: AMBENCH-F32
---

# DEC-067 — F32 PASS; select `mds2-3662` for independent rapid-turnaround falsification
# DEC-067 — F32 PASS; 독립 rapid-turnaround 반증 후보로 `mds2-3662` 선정

## Decision / 결정

**`PASS_F32_OUTCOME_UNSEEN_INDEPENDENT_FALSIFICATION_CANDIDATE`**.

Select NIST `mds2-3662` as `PRIMARY_F32` for the next separately preregistered low-DOF numerical experiment.

The source qualifies because it provides:
- an independent IN625 physical experiment chain;
- converging vs diverging rapid-turnaround scan-strategy conditions;
- three physical samples per primary condition in Set 2;
- interpretable top-surface melt-pool width/area outcomes;
- current reproducible checksum-bearing source identity;
- no numerical candidate outcomes/effects emitted by the prior F26 source qualification.

## Non-overclaim boundary / 과장 금지 경계

`mds2-3662` does **not** manipulate the same condition as AMB2025-07's `0.75 ms vs 5.0 ms` turnaround-time experiment. Therefore the next experiment is an **independent scan-strategy falsification/mechanistic-transfer test**, not a same-construct replication.

Publication/design context was previously used for source qualification, so descendant work must not claim pristine literature-level hypothesis blindness. Freeze a nondirectional/two-sided primary inference unless a separate pre-outcome physical direction is justified prospectively.

## Exact next authorized mission action / 정확한 다음 승인 mission action

**`AMBENCH-E33 — IN625 converging-vs-diverging rapid-turnaround melt-pool geometry falsification`**.

Before numerical outcome access, E33 must complete a documentation/schema-only preflight and freeze exact sample IDs, track-count snapshots, operator nesting, primary/sensitivity endpoint, low-DOF statistic, missing/outlier handling, and immutable source hashes.

## Capability / Portfolio / 비용
`DEC-055` Continuity Overlay remains active. Reuse existing source-integrity/preregistration/exact-small-n patterns; classification remains `SHARED-INTERNAL-CANDIDATE`. Incremental monetary cost: `0 USD`; potentially billable work requires explicit prior approval.
