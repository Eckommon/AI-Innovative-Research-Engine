---
id: DEC-091
type: decision
created: 2026-09-03
status: ACTIVE
related:
  - AMBENCH-F45
  - Issue #63
  - DEC-090
---

# DEC-091 — Execute Frozen F45 Checksum-Preserving Range Ingress Gate
# DEC-091 — 고정 F45 Checksum-Preserving Range Ingress Gate 실행

## Decision / 결정

Execute F45 exactly as preregistered using standard public GitHub Actions only.

## Frozen execution envelope / 고정 실행 범위

- metadata: official NIST NERDm `mds2-2507` only;
- component identity: `RHF_Command.zip`, 18,079,576 bytes, frozen SHA-256;
- fixed chunk size: 1,048,576 bytes;
- 18 sequential ranges;
- exact HTTP 206 + `Content-Range` + body-length validation;
- maximum 3 attempts per unchanged range;
- 90 s read timeout per attempt;
- fixed 3 s retry delay;
- workflow hard cap: **15 minutes**;
- no adaptive chunking, concurrency, mirror, whole-object fallback, alternate endpoint, raw artifact persistence, simulator or scientific outcome access.

## Fail-closed / 실패 폐쇄

- deterministic range-protocol semantic incompatibility => `REJECT_F45_RANGE_PROTOCOL_NOT_SUPPORTED`;
- metadata/source drift, repeated transport errors, or workflow cap => `HOLD_F45_SOURCE_OR_NETWORK`;
- only exact reconstructed size/SHA plus ZIP/P01 identity may produce `PASS_F45_CHECKSUM_PRESERVING_RANGE_INGRESS`.

A GitHub Actions job success does not override the scientific/source gate written in `research/AMBENCH-F45/RESULT.md`.

## Cost / 비용

Incremental monetary cost remains `0 USD`. No billable service is authorized.
