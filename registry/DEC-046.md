---
id: DEC-046
type: decision
state: ACTIVE
created: 2026-08-23
source_of_truth: github
---

# DEC-046 — F20 passes workbook immutable-schema gate / F20 workbook immutable-schema gate PASS

**Decision / 결정:** `AMBENCH-F20` final gate = **`PASS_F20_WORKBOOK_IMMUTABLE_SCHEMA_READY`**.

**KO:** Workbook authoritative NERDm checksum과 transient local SHA-256이 일치하고, numerical outcome을 출력하지 않은 schema-only inspection이 16개 part sheet mapping을 확립했다. Frozen `XYPT_L101-L125.zip`도 current NERDm exact component identity·SHA-256·official URL이 확립되어 F19 segmentation의 authoritative future validation path가 유지된다. Direct XYPT sidecar/ZIP byte retrieval은 실패했으므로 numerical segmentation validation 자체는 아직 허가되지 않는다.

E19 numerical experiment는 자동 시작하지 않는다. 먼저 workbook columns A/B 및 XCT endpoint의 authoritative semantics를 outcome-blind 상태에서 고정하고, future XYPT bytes가 NERDm SHA-256과 일치함을 검증한 후에만 별도 preregistration 하에서 진행한다.

**EN:** The workbook's authoritative NERDm checksum matches its transiently computed local SHA-256, and schema-only inspection without numerical outcome emission establishes deterministic sixteen-part sheet mapping. Current NERDm also establishes exact component identity, SHA-256 and official URL for frozen `XYPT_L101-L125.zip`, preserving an authoritative future validation path for the F19 segmentation rule. Direct XYPT sidecar/ZIP byte retrieval failed, so numerical segmentation validation is not yet authorized.

Do not automatically start the E19 numerical experiment. First freeze authoritative semantics for workbook columns A/B and the XCT endpoint while outcome-blind, and verify future XYPT bytes against the NERDm SHA-256 before any separately preregistered E19 execution.
