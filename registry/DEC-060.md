---
id: DEC-060
type: decision
state: ACTIVE
created: 2026-08-23
source_of_truth: github
research: AMBENCH-E29
---

# DEC-060 — Preregister E29 plate-level reconstructed overlap-depth experiment / E29 plate-level 재구성 overlap-depth 실험 사전등록

## Decision / 결정

**KO:** F28 source-readiness PASS 이후 다음 numerical mission work를 `AMBENCH-E29`로 고정한다. E29는 T72/T82/T92 대 T102/T112/T122의 P1 overlap depth를 NIST가 문서화한 pixel-coordinate + surface-reference + physical-scale contract로 재구성하고, physical plate를 독립 반복단위로 하는 exact 3-vs-3 one-sided permutation experiment로 실행한다.

**EN:** After the F28 source-readiness PASS, freeze the next numerical mission work as `AMBENCH-E29`. E29 reconstructs P1 overlap depth for T72/T82/T92 versus T102/T112/T122 using the NIST-documented pixel-coordinate + surface-reference + physical-scale contract and evaluates the result with an exact one-sided 3-vs-3 permutation experiment at the physical-plate level.

## Frozen design authority / 고정 설계 권위
The complete frozen design is `research/AMBENCH-E29/README.md`.

Key fixed elements:
- independent unit = physical plate;
- P1 only;
- direction = `0.75 ms > 5.0 ms`, inherited from E27 before the parser incident;
- primary plate endpoint = arithmetic mean of valid reconstructed P1 overlap depths;
- >=41/45 valid tracks required per plate;
- exact 20-allocation one-sided permutation;
- plate-level rank-biserial strong threshold `>=7/9`;
- common-valid sensitivity requires >=36/45 common tracks and positive `Delta_common` for strong PASS;
- no P2/P3 rescue, imputation, sign flip, source substitution, track pseudo-replication, or model escalation.

## Exposure disclosure / 노출 고지
`NEW_E29_NUMERICAL_OUTCOME_BLIND = NO__INHERITED_E27_SCHEMA_PREFLIGHT_GIBBERISH_EMISSION`.

E29 is a preregistered reconstruction-confirmation experiment under inherited exposure limitation, not a claim of fresh outcome blindness. The endpoint/direction/statistical framework was frozen before the E27 incident and is not re-selected here.

## Authorized execution / 승인된 실행
After this preregistration is committed and active state is synchronized, E29 may execute using zero-cost public standard GitHub-hosted runners and transient official NIST source bytes only.

Any source/hash/binding/scale/coverage failure => `HOLD_E29_INTEGRITY_OR_COVERAGE`; do not redesign inside E29.

## Capability / Portfolio
No new Skill/MCP/Plugin is required. The reusable source-integrity/reconstruction/evidence workflow remains `SHARED-INTERNAL-CANDIDATE`. Shared resource availability is not assumed without canonical ledger evidence.

## Cost / 비용
Incremental monetary cost must remain `0 USD`. Potentially billable work requires explicit prior user approval.
