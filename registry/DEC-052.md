---
id: DEC-052
type: decision
state: ACTIVE
created: 2026-08-23
source_of_truth: github
research: AMBENCH-D25
---

# DEC-052 — Stop same-representation escalation after D25 block-dominant diagnosis
# DEC-052 — D25 block 지배 진단 후 동일 representation 고도화를 중단한다

## Decision / 결정

**KO:** `AMBENCH-D25`의 frozen gate를 `D25_BLOCK_DOMINANT_HIERARCHICAL_STRUCTURE`로 확정한다. E24의 materiality failure(`partial R²=0.019321`)와 D25의 outcome block dominance(`block-only R²=0.998820`)를 함께 고려하여, 동일 `mds2-3761` aggregate representation에서 추가 feature fishing, endpoint switching, nonlinear/high-capacity model escalation을 진행하지 않는다.

**EN:** Finalize `AMBENCH-D25` as `D25_BLOCK_DOMINANT_HIERARCHICAL_STRUCTURE`. Given E24's materiality failure (`partial R²=0.019321`) together with D25's block-dominant outcome structure (`block-only R²=0.998820`), do not continue feature fishing, endpoint switching, or nonlinear/high-capacity model escalation on the same `mds2-3761` aggregate representation.

## Consequence / 후속
The next eligible route is an independently varied condition/dataset qualification or experiment design where:
- process variation is not primarily a deterministic part/block proxy;
- structural outcomes have independently interpretable variation;
- replication/condition structure can support a new mechanistic hypothesis.

Any future return to `mds2-3761` requires a new independent scientific rationale and separate preregistration; E24/D25 cannot be re-mined to rescue a positive result.

Cost governance remains unchanged: any potentially billable action requires explicit prior user approval.