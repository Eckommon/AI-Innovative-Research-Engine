---
id: DEC-023
type: decision
state: ACTIVE
created: 2026-08-22
updated: 2026-08-22
source_of_truth: github
related:
  - research/AMBENCH-E09/RESULT.md
  - Issue #24
  - CLM-033
  - CLM-034
  - CLM-035
  - DEC-022
---

# DEC-023 — Finalize AMBENCH-E09 as INCONCLUSIVE_CASE_LEVEL / E09 최종 종료 결정

**KO:** `AMBENCH-E09`를 **`COMPLETED — INCONCLUSIVE_CASE_LEVEL`**로 종료한다. exact ZIP archive evidence로 case `3.2` third-repeat file identity는 E09 분석 수준에서 해결됐지만 F08 summary CSV의 provenance inconsistency 자체는 보존한다. BP4 coupling-weighted VED는 process-only VED의 7-case 순위를 전혀 변경하지 않아 모든 frozen endpoint에서 `delta_rho=0`이었다. primary thermal은 두 predictor 모두와 `rho=0.0714286`, 세 factor axis 중 `2/3` concordant이므로 더 강한 positive/negative frozen label은 정당화되지 않는다.

**EN:** Finalize `AMBENCH-E09` as **`COMPLETED — INCONCLUSIVE_CASE_LEVEL`**. Exact ZIP archive evidence resolves the case `3.2` third-repeat file identity for E09 analysis while preserving the F08 summary-CSV provenance inconsistency. BP4 coupling-weighted VED did not change the seven-case rank of process-only VED, producing `delta_rho=0` for every frozen endpoint. The primary thermal endpoint has `rho=0.0714286` with both predictors and `2/3` factor-axis concordance, so no stronger frozen positive or negative label is justified.

## Boundaries / 경계

- Do not claim dynamic coupling is useless or redundant in general. / coupling 일반 무용성 주장 금지.
- Do not call BP1↔BP4 paired tracks/sensors. / paired 주장 금지.
- Do not promote secondary depth `rho=1.0` as coupling-specific because process-only is also `1.0`. / coupling 고유효과 아님.
- Do not tune E09 thresholds/features or escalate model capacity inside E09. / 사후 tuning·고용량화 금지.
- Any follow-up must test a **different preregistered scientific relationship**, e.g. magnitude-sensitive coupling relationship, within-BP4 temporal dynamics, or independent process conditions; none is automatically authorized. / 후속은 별도 사전등록.

`RAW-001` / `RAW_DATA_TRANSIENT_ONLY` remains the project-wide default for external raw public data. / 원천데이터 일시처리 원칙 지속 적용.

**Status:** `ACTIVE — E09 FINALIZED`.
