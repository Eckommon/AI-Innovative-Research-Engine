---
id: DEC-090
type: decision
created: 2026-09-03
status: ACTIVE
related:
  - AMBENCH-F44
  - Issue #62
  - CLM-116
  - DEC-089
---

# DEC-090 — Finalize F44 as Source-Ingress HOLD; Separate Transfer Qualification from Representation Testing
# DEC-090 — F44를 Source-Ingress HOLD로 종결하고 전송 자격검증과 표현 테스트를 분리

## Decision / 결정

Finalize AMBENCH-F44 as **`HOLD_F44_RUNTIME_OR_INTEGRITY`**. The representation-equivalence question remains unanswered because the checksum-frozen NIST archive could not be completely transferred under F44's preregistered bounded whole-object fetch mechanism.

Do not rerun F44 by silently increasing attempts/timeouts or changing transfer semantics inside the experiment.

## Interpretation / 해석

- workflow execution integrity: PASS — the workflow completed and persisted the scientific HOLD;
- source-ingress integrity: HOLD — no complete checksum-verified archive was obtained;
- simulator execution: NOT RUN;
- representation equivalence: NOT TESTED;
- path-order added value: NOT TESTED.

## Next authority / 다음 권한

Authorize a separate source-only gate, **AMBENCH-F45 — Checksum-Preserving Resumable Source-Ingress Qualification**, to determine whether the exact same NIST component can be acquired on a standard public runner using a prospectively frozen resumable/range transfer protocol while reproducing the exact expected byte size and SHA-256.

F45 must:
- use no simulator and no performance outcome;
- preserve NIST component identity exactly;
- use a fixed, preregistered transfer protocol and bounded retries;
- fail closed if HTTP range semantics are unsupported or final size/SHA differs;
- persist only transfer diagnostics/checksum evidence, not the raw archive;
- remain `0 USD` incremental cost.

A F45 PASS may authorize a new, separately numbered representation-equivalence experiment using the qualified ingress method. It does not retroactively change F44.

## Cost / 비용

Incremental monetary cost remains `0 USD`; any billable transfer/compute requires explicit prior user approval.
