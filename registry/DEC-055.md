---
id: DEC-055
type: decision
state: ACTIVE
created: 2026-08-23
source_of_truth: github
---

# DEC-055 — Adopt GPT↔GitHub↔Agent Capability Integration v2.1 as a Continuity Overlay / GPT↔GitHub↔Agent Capability Integration v2.1을 지속 운영 Overlay로 채택

## Decision / 결정

**KO:** 사용자가 전달한 「GPT Project ↔ GitHub ↔ Agent Capability Integration Audit & Upgrade Master Prompt v2.1」의 운영 원칙을 이 프로젝트의 **지속 운영·고도화 Overlay Protocol**로 채택한다. 이 overlay는 기존 mission, research chain, frozen decisions, canonical governance, cost/human gates를 대체하거나 reset하지 않는다. 현재 mission work를 우선 계속하면서 필요한 state-integrity, GPT-operability, capability-modularity, verification 개선을 checkpoint 단위의 최소 delta로 흡수한다.

**EN:** Adopt the operating principles of the user-provided “GPT Project ↔ GitHub ↔ Agent Capability Integration Audit & Upgrade Master Prompt v2.1” as this project's **continuity and upgrade overlay protocol**. The overlay does not replace or reset the existing mission, research chain, frozen decisions, canonical governance, or cost/human gates. Mission work remains primary while state-integrity, GPT-operability, capability-modularity, and verification improvements are absorbed as minimal checkpoint-level deltas.

## Existing functional equivalents / 기존 기능적 equivalent

A new root `AGENTS.md` is **not required now**. The Minimum Operability Baseline is already functionally recoverable through existing canonical records:

- Mission / Scope → `README.md`, `docs/GOVERNANCE.md`
- Authority / Agent operating rule → `docs/GOVERNANCE.md`, `docs/GPT_GITHUB_SYNC_PROTOCOL.md`, `docs/HALLUCINATION_CONTROL_PROTOCOL.md`
- Current State / Active Work / Next Action → `STATUS.md`, `context/SESSION_HANDOFF.md`, live Issues
- Human / Cost / Security Gate → `docs/NO_COST_POLICY.md`, `COST-001`, `DEC-028`, governance
- Decision / Evidence Authority → `registry/DECISION_LOG.md`, `registry/CLAIM_LEDGER.md`, standalone `DEC-*` / `CLM-*` records
- Verification Method → preregistered research gates + GitHub Actions + `state-integrity.yml`
- Write-back / Continuation → `STATUS.md`, `context/SESSION_HANDOFF.md`, sync/hallucination-control protocols

No duplicate instruction file is created unless a future verified `MISSING-BLOCKING` condition appears.

## Immediate P0 correction / 즉시 P0 보정

The README's dynamic “Current Baseline” section was found stale at F08 while canonical `STATUS.md`/`SESSION_HANDOFF.md` were at F26. This is a Source-of-Truth drift risk for cold-start agents. The durable fix is to remove duplicated dynamic current-state values from README and point to `STATUS.md` + `SESSION_HANDOFF.md` as the canonical live-state authorities.

## Capability classification / Capability 분류

Current recurring workflow:

`read → reconcile → preregister → source-integrity check → zero-cost execute → verify → claims/decision write-back → STATUS/HANDOFF re-read`

is classified **`SHARED-INTERNAL-CANDIDATE`**, not yet a new Skill/MCP/Plugin/Hosted Capability. Before extraction, check the existing Central Capability Repository for overlap and define a stable contract/eval only if cross-project reuse is verified.

Specific candidates for later portfolio reconciliation:
- GitHub state/checkpoint reconciliation;
- research preregistration + frozen-gate evaluation;
- NIST NERDm immutable-component qualification;
- evidence/provenance + cost-gate enforcement.

## Priority / 우선순위

`CURRENT MISSION WORK > integrity/safety P0 > capability upgrade > distribution`.

The next mission action remains **AMBENCH-E27**. Portfolio/capability reconciliation is not a prerequisite for E27.

## Cost / 비용

This adoption introduces no incremental monetary cost, no permission expansion, no visibility change, no destructive action, and no external paid service. Any future potentially billable capability or infrastructure action remains subject to `COST-001` / `DEC-028` and explicit prior user approval.