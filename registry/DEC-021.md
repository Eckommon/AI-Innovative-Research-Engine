---
id: DEC-021
type: decision
state: ACTIVE
created: 2026-08-22
updated: 2026-08-22
source_of_truth: github
related:
  - research/AMBENCH-E09/README.md
  - Issue #24
  - DEC-020
---

# DEC-021 — Preregister AMBENCH-E09 / AMBENCH-E09 사전등록 결정

**KO:** `AMBENCH-F08 = PARTIAL_CASE_LEVEL_READY`의 허용 경계 안에서, Issue #24 `AMBENCH-E09`를 **unpaired BP4 dynamic-coupling → BP1 thermal/geometry nominal-case-family ordering test**로 사전등록한다. BP1↔BP4 physical-track/repeat pairing은 금지하고, 동일 case label을 동일 조건으로 취급하지 않는다. 1차 질문은 BP4 coupling-weighted VED ordering이 BP4 process-only VED ordering보다 BP1의 `1298 °C` thermal occupancy case ordering과 더 잘 대응하는지 여부다. width/depth는 둘 다 secondary로 대칭 유지한다.

**EN:** Within the boundary established by `AMBENCH-F08 = PARTIAL_CASE_LEVEL_READY`, preregister Issue #24 `AMBENCH-E09` as an **unpaired BP4 dynamic-coupling → BP1 thermal/geometry nominal-case-family ordering test**. BP1↔BP4 physical-track/repeat pairing remains prohibited, and matching case labels are not treated as identical conditions. The primary question is whether BP4 coupling-weighted VED ordering corresponds to BP1 `1298 °C` thermal-occupancy case ordering better than BP4 process-only VED ordering. Width and depth are retained symmetrically as secondary endpoints.

## Integrity / 무결성

- `NEW_MODALITY_OUTCOME_BLIND = YES` — BP4 coupling values have not been accessed before preregistration.
- `FULL_OUTCOME_BLIND = NO — BP1_PREOBSERVED` — BP1 outcomes were observed in earlier E03/E05/D06 work and this limitation is explicit.
- case `3.2` third-repeat identity is not silently corrected; filename-only preflight is frozen before numeric access.
- unresolved surface roughness conflict is excluded from harmonized covariates.
- no causal, paired-sensor, or deployable-prediction claim is authorized by E09.

## Frozen full-positive gate / 고정 full-positive gate

`CROSS_MODAL_ORDERING_SIGNAL` requires:
1. 7 analyzable case families;
2. `rho_coupled(Y_thermal) >= 0.70`;
3. `delta_rho_thermal >= +0.20` versus BP4 process-only VED ordering;
4. thermal factor-axis sign concordance `3/3`;
5. no integrity failure.

Other frozen final outcomes are defined in `research/AMBENCH-E09/README.md`.

## Cost / 비용

`COST-001` remains controlling: execution must have zero incremental monetary cost; no paid API/data/cloud/GPU/larger runner is authorized.

**Status:** `ACTIVE — PREREGISTERED`.
