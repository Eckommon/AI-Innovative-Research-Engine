---
id: DEC-059
type: decision
state: ACTIVE
created: 2026-08-23
source_of_truth: github
research: AMBENCH-F28
---

# DEC-059 — F28 PASS; authorize separate preregistered plate-level numerical experiment / F28 PASS; 별도 사전등록 plate-level numerical experiment 허용

## Decision / 결정

**`PASS_F28_PLATE_SPECIFIC_GEOMETRY_SOURCE_READY`**

**KO:** 여섯 target plate의 P1 pixel annotation, NIST의 authoritative reconstruction semantics, physical image scaling contract, 그리고 Micrographs surface-reference table의 one-to-one P1 binding이 검증되었다. 따라서 plate-specific P1 geometry를 결정론적으로 재구성할 수 있는 source/schema/provenance route를 PASS로 판정한다.

**EN:** The six target plates' P1 pixel annotations, NIST authoritative reconstruction semantics, physical image-scaling contract, and one-to-one P1 binding in the Micrographs surface-reference table are verified. Therefore the source/schema/provenance route for deterministic plate-specific P1 geometry reconstruction passes.

## Scope of PASS / PASS 범위

This is a **source-readiness PASS only**. It is not evidence of a turnaround-time effect and does not authorize retrospective endpoint selection.

## Next authorized action / 다음 승인된 행동

The next eligible mission action is a **separate preregistration** for `AMBENCH-E29 — six-plate P1 reconstructed overlap-depth turnaround-time controlled experiment`.

Before any numerical coordinate/reference values are inspected, E29 must freeze at least:
- six physical plates and 0.75 ms / 5.0 ms grouping;
- P1 only;
- authoritative reconstruction formula and physical scale source;
- primary plate-level overlap-depth endpoint;
- track-level missingness/coverage rule;
- plate-level aggregation rule;
- directional hypothesis and exact permutation statistic;
- effect-size calculation and PASS/MIXED/NO_MATERIAL_GAIN/HOLD gates;
- dependence limitation: 45 tracks are nested measurements, not 45 independent replicates;
- permanent inherited E27 exposure disclosure.

No numerical experiment may begin until that separate preregistration is committed.

## Capability / Portfolio delta

The NERDm immutable-component + bounded-schema + provenance workflow remains `SHARED-INTERNAL-CANDIDATE`. Do not create a duplicate Skill/MCP/Plugin. Shared-content/infrastructure/resource reconciliation is nonblocking and must not invent common budget/quota.

## Cost / 비용

Incremental monetary cost for F28: `0 USD`. Any potentially billable next action remains subject to `COST-001` / `DEC-028` and explicit prior approval.
