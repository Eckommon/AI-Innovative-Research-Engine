# AMBENCH-F08 metadata execution trigger

Execution-only trigger for the preregistered outcome-blind `mds2-3842` PDR metadata inspection.

Run 2 adds only version-lineage manifests plus the 496-byte `summary_of_data_files.csv` needed to recover repeat filenames and process-parameter mapping. No coupling time-series values, paid resources, GPU/larger runner, or artifact upload are authorized.
