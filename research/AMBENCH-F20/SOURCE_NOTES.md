# AMBENCH-F20 Source Notes / 소스 노트

Pre-retrieval observations frozen after preregistration:

- Current Data.gov metadata identifies XCT dataset `ark:/88434/mds2-2514` and exposes official workbook and `.sha256` download URLs.
- NIST metrics reports `OverhangX16_ImageHistograms.xlsx` at approximately 193.26 KB.
- NIST PDR landing page resolves for `mds2-2514` but the browser-facing page is dynamically loaded.
- NIST documents a Resource Metadata Management (RMM) API and NERDm machine-readable metadata system; F20 will attempt to use these official metadata routes before repeating direct component downloads.
- No numerical workbook cells have been accessed.
