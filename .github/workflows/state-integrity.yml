name: State Integrity / 상태 무결성
# US-FCC-ULS-F01 terminal-state recheck: verify frozen 9/18 HOLD, closed Issue #166, and portfolio-reselection checkpoint.
# PORTFOLIO-R40 manual-canonical-sync recheck: DEC-241 / checkpoint mirrors.
# US-IRS-EO-F01 terminal-state recheck: verify canonical PASS/N01-authorized state after bot-authored finalizer push suppression.
# Verification trigger: terminal-state push recheck after the Issue-event run remained queued.
# E01 terminal-state recheck: verify canonical post-finalization state after bot-authored push suppression.
# US-MINE-F01 terminal-state recheck: verify canonical post-finalization state after bot-authored push suppression.
# C-EU-F01 terminal-state recheck: verify canonical post-finalization state after bot-authored push/issue suppression.
# PORTFOLIO-R30 terminal-state recheck: verify canonical post-finalization state after bot-authored push/issue suppression.
# US-FDA-MD-F01 terminal-state recheck: verify canonical post-finalization state after bot-authored push/issue suppression.
# PORTFOLIO-R31 terminal-state recheck: verify canonical post-finalization state after bot-authored push/issue suppression.
# US-NHTSA-MC-F01 terminal-state recheck: verify canonical post-finalization state after bot-authored push/issue suppression.
# PORTFOLIO-R32 terminal-state recheck: verify canonical post-finalization state after bot-authored push/issue suppression.
# US-FMCSA-HAZ-F01 terminal-state recheck: verify canonical post-finalization state after bot-authored push/issue suppression.
# PORTFOLIO-R33 terminal-state recheck: verify canonical post-finalization state after bot-authored push/issue suppression.
# US-FTA-TRANSIT-F01 terminal-state recheck: verify canonical post-finalization state after bot-authored push/issue suppression.
# US-FTA-TRANSIT-N01 terminal-state recheck: verify canonical post-finalization state after bot-authored push/issue suppression.
# PORTFOLIO-R34 terminal-state recheck: verify canonical post-finalization state after bot-authored push/issue suppression.
# US-EPA-XMEDIA-F01 terminal-state recheck: verify corrected canonical post-finalization state after bot-authored push/issue suppression.
# US-EPA-XMEDIA-N01 terminal-state recheck: verify canonical post-finalization state after bot-authored push/issue suppression.
# US-EPA-XMEDIA-E01 terminal-state recheck: verify canonical no-positive terminal after bot-authored push/issue suppression.
# PORTFOLIO-R35 terminal-state recheck: verify canonical FAA-AIP selection after bot-authored push/issue suppression.
# US-FAA-AIP-F01 terminal-state recheck: verify canonical frozen state-concordance HOLD after bot-authored push/issue suppression.
# US-FSIS-SAMPLE-F01 terminal-state recheck: verify transport-blocked terminal state is distinct from scientific HOLD.

on:
  push:
    branches: [main]
    paths:
      - 'context/checkpoint.json'
      - 'tools/check_checkpoint.py'
      - 'STATUS.md'
      - 'context/SESSION_HANDOFF.md'
      - 'context/PROJECT_MEMORY.md'
      - 'registry/DECISION_LOG.md'
      - 'docs/HALLUCINATION_CONTROL_PROTOCOL.md'
      - 'docs/GPT_GITHUB_SYNC_PROTOCOL.md'
      - '.github/workflows/state-integrity.yml'
  pull_request:
    branches: [main]
    paths:
      - 'context/checkpoint.json'
      - 'tools/check_checkpoint.py'
      - 'STATUS.md'
      - 'context/SESSION_HANDOFF.md'
      - 'context/PROJECT_MEMORY.md'
      - 'registry/DECISION_LOG.md'
      - 'docs/HALLUCINATION_CONTROL_PROTOCOL.md'
      - 'docs/GPT_GITHUB_SYNC_PROTOCOL.md'
      - '.github/workflows/state-integrity.yml'
  issues:
    types: [opened, reopened, closed]
  workflow_dispatch:

permissions:
  contents: read
  issues: read

jobs:
  checkpoint-integrity:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - name: Verify canonical checkpoint and decision index
        run: python tools/check_checkpoint.py

      - name: Verify synchronized checkpoint and live Issue state
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPOSITORY: ${{ github.repository }}
        run: |
          python - <<'PY'
          from pathlib import Path
          import json
          import os
          import urllib.request

          REQUIRED = [
              'checkpoint_id',
              'active_issue',
              'active_research',
              'last_completed_issue',
              'last_completed_research',
              'last_decision',
              'updated',
          ]

          def front_matter(path):
              text = Path(path).read_text(encoding='utf-8')
              lines = text.splitlines()
              if not lines or lines[0].strip() != '---':
                  raise SystemExit(f'{path}: missing YAML-style front matter')
              data = {}
              for line in lines[1:]:
                  if line.strip() == '---':
                      break
                  if not line.strip() or line[:1].isspace() or ':' not in line:
                      continue
                  k, v = line.split(':', 1)
                  data[k.strip()] = v.strip().strip('"').strip("'")
              missing = [k for k in REQUIRED if not data.get(k)]
              if missing:
                  raise SystemExit(f'{path}: missing checkpoint fields: {missing}')
              return data

          status = front_matter('STATUS.md')
          handoff = front_matter('context/SESSION_HANDOFF.md')

          for key in REQUIRED:
              if status[key] != handoff[key]:
                  raise SystemExit(
                      f'CHECKPOINT_MISMATCH {key}: STATUS={status[key]!r} HANDOFF={handoff[key]!r}'
                  )

          token = os.environ['GH_TOKEN']
          repo = os.environ['GH_REPOSITORY']

          def api(path):
              req = urllib.request.Request(
                  f'https://api.github.com/repos/{repo}{path}',
                  headers={
                      'Authorization': f'Bearer {token}',
                      'Accept': 'application/vnd.github+json',
                      'X-GitHub-Api-Version': '2022-11-28',
                  },
              )
              with urllib.request.urlopen(req, timeout=20) as response:
                  return json.load(response)

          open_items = api('/issues?state=open&per_page=100')
          open_issues = [item for item in open_items if 'pull_request' not in item]
          open_numbers = {int(item['number']) for item in open_issues}

          active_raw = status['active_issue'].lower()
          if active_raw in {'none', 'null', 'n/a'}:
              if open_numbers:
                  raise SystemExit(
                      'STATE_DRIFT_DETECTED: active_issue=none but open research Issue(s) exist: '
                      + ','.join(map(str, sorted(open_numbers)))
                  )
          else:
              try:
                  active_number = int(status['active_issue'].lstrip('#'))
              except ValueError as exc:
                  raise SystemExit(f'Invalid active_issue: {status["active_issue"]!r}') from exc
              if open_numbers != {active_number}:
                  raise SystemExit(
                      f'STATE_DRIFT_DETECTED: declared active Issue #{active_number} is not open; '
                      f'open Issues={sorted(open_numbers)}'
                  )

          try:
              last_number = int(status['last_completed_issue'].lstrip('#'))
          except ValueError as exc:
              raise SystemExit(
                  f'Invalid last_completed_issue: {status["last_completed_issue"]!r}'
              ) from exc

          last_issue = api(f'/issues/{last_number}')
          if 'pull_request' in last_issue or last_issue.get('state') != 'closed':
              raise SystemExit(
                  f'STATE_DRIFT_DETECTED: last_completed_issue #{last_number} is not a closed Issue'
              )

          research_id = status['last_completed_research']
          result_path = Path('research') / research_id / 'RESULT.md'
          readme_path = Path('research') / research_id / 'README.md'
          if not result_path.exists() and not readme_path.exists():
              raise SystemExit(
                  f'STATE_DRIFT_DETECTED: no durable research artifact for {research_id}'
              )

          print('STATE_INTEGRITY_PASS')
          print('checkpoint_id=', status['checkpoint_id'])
          print('active_issue=', status['active_issue'])
          print('last_completed_issue=', status['last_completed_issue'])
          print('last_completed_research=', status['last_completed_research'])
          print('last_decision=', status['last_decision'])
          PY

      - name: Confirm COST-001 boundary
        run: |
          echo 'State-integrity uses a standard public-repository GitHub-hosted runner only.'
          echo 'No paid API, larger/GPU runner, or artifact upload is used.'
