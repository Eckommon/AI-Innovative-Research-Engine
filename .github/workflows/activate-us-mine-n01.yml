name: Activate US-MINE-N01 Canonical State

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/activate-us-mine-n01.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  activate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Verify authorization inputs
        run: |
          python -m py_compile tools/activate_us_mine_n01.py
          python - <<'PY'
          import json
          from pathlib import Path
          c=json.loads(Path('context/checkpoint.json').read_text())
          assert c['active_issue'] == 'none'
          assert c['last_completed_issue'] == 134
          assert c['last_decision'] == 'DEC-184'
          r=Path('research/US-MINE-N01/README.md').read_text()
          assert 'state: AUTHORIZED_OUTCOME_BLIND_DESIGN' in r
          assert 'accident_source_read: false' in r
          assert 'ALL_SECTOR_OPERATOR_HOURS_RAMP_UP' in r
          assert Path('registry/DEC-185.md').exists()
          print('US_MINE_N01_AUTHORIZATION_INPUT_VERIFY_PASS')
          PY
      - name: Activate canonical state
        run: python tools/activate_us_mine_n01.py
      - name: Verify canonical mirrors
        run: |
          python tools/check_checkpoint.py
          python - <<'PY'
          import json
          from pathlib import Path
          c=json.loads(Path('context/checkpoint.json').read_text())
          assert c['active_issue'] == 135
          assert c['active_research'] == 'US-MINE-N01'
          assert c['last_completed_issue'] == 134
          assert c['last_decision'] == 'DEC-185'
          assert '`DEC-185`' in Path('registry/DECISION_LOG.md').read_text()
          assert 'US_MINE_N01_ACTIVE__OUTCOME_BLIND_HOURS_RAMP_DESIGN' in Path('STATUS.md').read_text()
          print('US_MINE_N01_CANONICAL_ACTIVATION_VERIFY_PASS')
          PY
      - name: Commit activation
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add STATUS.md context/SESSION_HANDOFF.md context/checkpoint.json registry/DECISION_LOG.md
          git commit -m 'Activate US-MINE-N01 outcome-blind hours-ramp design'
          git pull --rebase origin main
          git push origin HEAD:main
      - name: Confirm zero-cost boundary
        run: |
          echo 'Standard public-repository GitHub-hosted runner only.'
          echo 'Incremental monetary cost: 0 USD.'
